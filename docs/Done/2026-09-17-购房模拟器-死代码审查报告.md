# 购房模拟器 · 重构后死代码审查报告

- 审查对象：`miniapp/pages/house-simulator/**`（38 屏：购房 24 + 装修 14）
- 审查基准：HEAD `9893c902` *feat(house-sim): 重构购房模拟器流程与样式，新增装修模块适配*（37 文件、+6560/−5113）
- 审查日期：2026-09-17
- 结论：**已删除 24 项确认无误的死代码（−106 行 / +23 行）**；另有 5 类「有引用但可疑」与 2 项重构回归**未动**，见 §4 / §5。
- 回归验证：`tsc --noEmit` 零错误（含 `noUnusedLocals`/`noUnusedParameters` 的模块级审计配置）、`vitest` **495/495 全绿**（与重构后基线一致）、38 屏冒烟 45 例 + handlers 41 例 + matrix 94 例全绿。

## 1. 审查方法（证据链）

| 手段 | 覆盖的"死" | 备注 |
|---|---|---|
| `tsc --noEmit --noUnusedLocals --noUnusedParameters`（模块级 tsconfig，`/tmp` 内） | 未使用的导入、局部变量、类型导入 | 机器判定，零误报 |
| 全量导出符号 × 全仓引用计数脚本 | 未引用的函数 / 常量 / 类型 / 配置字段 | 区分"跨文件引用"与"仅同文件自用" |
| `SimState` 与各配置接口字段的读/写计数脚本 | 只写不读的状态字段、只配置不消费的配置项 | 识别 `payKind` / `thumbCls` 这类"看着在用"的死字段 |
| 块类型产出（TS `t:`）↔ 模板分支（WXML `item.t ===`）双向比对 | 永不渲染的块分支 | **结果为空集**：32 个块类型两侧完全一致 |
| wxss 选择器 ↔ wxml `class` + TS 动态类名比对 | 无消费者的样式规则 | 排除后代选择器误报（如 `.brow .k`） |
| `--allowUnreachableCode false` | 语句级不可达代码 | **零命中**，另有 5 处"防御性不可达分支"见 §4.3 |

## 2. 已删除（确认无误）

> 下表行号均为**改动前（`9893c902`）**的行号，便于对照原始代码；删除位置可用 `git diff` 复核。

### 2.1 未使用的导入与局部变量（11 项，tsc 直判）

| 文件:行号 | 符号 | 类型 |
|---|---|---|
| `utils/scenes-money.ts:15` | `bubble` | 未使用导入（`scenes-common`） |
| `utils/scenes-money.ts:82` | `const h = houseOf(S)!` | 未使用局部变量（`sceneFunds`） |
| `utils/scenes-money.ts:251` | `const d = screenMeta(key)` | 未使用局部变量（`checkScreen`） |
| `utils/scenes-loan.ts:13` | `INCOME` | 未使用导入（`constants`） |
| `utils/scenes-loan.ts:62` | `const m = money(S)` | 未使用局部变量（`sceneLoanChk`） |
| `utils/scenes-nego.ts:47` | `const h = houseOf(S)!` | 未使用局部变量（`sceneNego2`） |
| `utils/scenes-renov.ts:16` | `fmtN` | 未使用导入（`calc`） |
| `utils/scenes-renov.ts:37` | `RenovBill` | 未使用类型导入（`renov-data`） |
| `utils/scenes-close.ts:15` | `OptItem` | 未使用类型导入（`scenes-common`） |
| `index/index.ts:33` | `ModalData` | 未使用类型导入（`render`） |
| `index/index.ts:36` | `HandlerCtx` | 未使用类型导入（`handlers`） |

### 2.2 死符号与死配置项（6 项 / 22 处）

| 文件:行号 | 符号 | 证据 |
|---|---|---|
| `utils/renov-data.ts:406` | `RENOV_ALL`（+ 405 行注释） | 全仓 0 引用；注释写"装修总账屏用"，但总账屏已改用 `RENOV_STAGES.filter(...)` 自算 |
| `utils/constants.ts:272-273` | `SimState.payKind`（含 `:503` 初始值、`:19` `PayKind` 导入） | 全仓 0 读 0 写；付款确认弹窗已迁到页面 `data.modal`（`render.emptyModal/payModal`） |
| `utils/constants.ts:101` | `House.type`（+ 6 条 HOUSES 赋值 `335/343/351/359/367/375`、`calc.ts:160` `c.type = "二手房"`） | 0 读；"新房"语义已由 `hold: "new"` 承担 |
| `utils/constants.ts:106` | `House.thumbCls`（+ 同上 6 条 HOUSES、`calc.ts:150`） | 0 读；`thumb-a/b/c` 全仓无任何样式定义，是彻底的悬空配置 |
| `utils/constants.ts:119-121` | `House.seller` / `sellerTag` / `intro`（+ HOUSES `337/345/353/361/369/377`、`338/346/354/362/370/378`、`calc.ts:152-153`） | 0 读；房源卡模板无对应节点（重构前 `9893c902^` 的 wxml 也未使用） |
| `utils/handlers.ts:38` | `HandlerCtx.setData` | 三个处理器（setup/flow/renov）均只经 `data`/`render`/`nextScene`/弹层回调，从不调 `ctx.setData` |

> 说明：`House` 的 `seller/sellerTag/intro` 属**文案数据**删除。若后续要恢复"房东 / 急售 / 房源简介"展示，从 `git show 9893c902^:miniapp/pages/house-simulator/utils/constants.ts` 可取回；已冻结的 `docs/design/*.html` 与 `docs/Done/购房模拟器.md` 未做改动。

### 2.3 无消费者的样式规则（7 项 / 61 行）

| 文件:行号 | 选择器 | 证据 |
|---|---|---|
| `index/styles/opts.wxss:22-27` | `.opt-t` | 模板用的是 `class="op-t"`（`flow.wxss:271` 已定义）——重构改名后遗留的旧名副本 |
| `index/styles/renov.wxss:122-139` | `.opt-tag` / `.opt-tag.hot` / `.opt-tag.cool` | 同上，模板用 `op-tag`（`flow.wxss:275/287/291`） |
| `index/styles/renov.wxss:57-60` | `.gm-of` | 模板无 `gm-of`；`flow.wxss` 里的 `.gm-v .gm-of` 同样零消费者 |
| `index/styles/renov.wxss:61-64` | `.gm-loss` | 模板用 `g-loss`（`flow.wxss:113`） |
| `index/styles/base.wxss:74-80` | `.steps-top` | 旧版流程条顶部行，现 HUD 改用 `.hud`（`flow.wxss`） |
| `index/styles/base.wxss:82-85` | `.steps-pos` | 同上，配套死类 |
| `index/styles/flow.wxss:110-112` + `index/styles/forms.wxss:55-58` + `:67-72` | `.gm-v .gm-of` / `.btn-rust` / `.btn-sm` | 按钮实际只用到 `btn-ink`（缺省）/ `btn-out` / `btn-disabled` |

配套文案同步：`utils/scenes-common.ts:266` 的注释原写"异常见 btn-out / btn-rust"，`btn-rust` 已不存在，改为"次级动作用 btn-out；禁用态由 disabled 自动走 btn-disabled"。

## 3. 未命中项（排查过，确认不是死代码）

- **块类型**：32 种 `SceneBlock` 的产出方（TS `t: "xxx"`）与渲染方（WXML `item.t === 'xxx'`）**双向完全一致**，无"永不渲染的分支"，`union` 也无多余成员。
- **样式文件**：`styles/` 下 11 份全部被 `index.wxss` `@import`；`.deed.wxss`（重构中被删的旧文件）已无任何 `@import` 残留。
- **事件绑定**：`data-act` 动作字符串（`n1:` / `n2:` / `lt:` / `ds:` / `bor:` / `cl:` / `ho:` / `pay:` / `renov*` 等）与 `handlers-*` 的分发前缀逐一比对，**无孤儿动作**（`payOk`/`payCancel`/`noop`/`onStage*` 均有实现）。
- **页面数据字段**：`PageData` 全部 16 个字段（`blocks`/`primary`/`guard`/`steps`/`stepPos`/`dayText`/`renovLabel`/`meters`/`stressEmoji`/`custBaseVisible`/`anchor`/`intoView`/`modal`/`formCash`/`custPrice`/`custArea`/`custBase`）均在 wxml 中被消费。
- **配置项**：`Role`、`RenovPkg`、`RenovTier`、`RenovStageDef`、`RenovContractItem`、`ScreenMeta`、`SignItem/SignOmit` 的字段逐个核对，除 §2.2 列出的外全部有读方。
- **埋雷可达性**：`SIGN_ITEMS[].omit.at` 与 `RENOV_CONTRACT[].omit.at/noMine.at` 指向的阶段 key 都存在且晚于其所在决策屏 → 不存在"永远不爆的雷"。
- **语句级不可达**：`--allowUnreachableCode false` 零命中。

## 4. 保留未删（建议你决定）

### 4.1 生产侧无引用、但**被单测引用**（3 项）

| 文件:行号 | 符号 | 现状 |
|---|---|---|
| `utils/calc.ts:284` | `avail()` | `return S.cash`，重构把 `sceneLoanChk` 的现金判断改为内联后失去生产引用；`calc.test.ts:294` 仍在断言 |
| `utils/calc.ts:321` | `loanPrincipal()` | 计算逻辑已内联进 `iloan()`；`calc.test.ts:323-331` 仍在断言 |
| `utils/render.ts:255` | `TIME_SCALE` | 只是 `flow.ts` 常量的再导出；`render.test.ts:318-319` 仍在断言 |

> 处理建议：连同 3 处断言一起删（属"测试在守护死代码"）；但按项目"唯一真源 = 代码 + 单测"的约定，我不擅自删测试。你说一声即可一并清理。

### 4.2 只写不读 / 仅测试断言的数据字段（2 项）

- `utils/constants.ts:241` `SimState.negoR2`：仅被 `handlers-flow.ts:66/79`、`handlers-setup.ts:35` 写入，**无任何读取**；`handlers.test.ts:282` 断言其为 `null`。可视为"预留的第二轮砍价记录"，删则需同步改测试。
- `utils/constants.ts:74` `Role.owned`：生产代码走 `ownedCount()`（按 `role.k` 判定），该字段无读取；`calc.test.ts:114` 以 `ROLES.trade.owned === 0` 作为"置换按首套认定"的护栏。**建议保留**（删掉会失去这条护栏）。

### 4.3 冗余 `export`（同文件内自用，跨文件 0 引用）

去掉 `export` 关键字纯属收敛公开面、无功能收益，本轮**刻意不动**以免制造无关 diff。清单：`riskLog.ts:15 readRiskLog`、`renov-data.ts:323 RENOV_PLAN_TOTAL`、`scenes-common.ts:282 timeStrip`、`scenes-start.ts:48 customDraftView`、`scenes-start.ts:225 customNote`、`handlers-renov.ts:94 settleContract`、`constants.ts:311 DEFAULT_CASH`，以及 `Money / QaDef / NegoR1Option / Nego2Option / MineItem / Judge / LoanState / SignOmit / HandItem / PayKindDef / MeterData / TimeMeterData / PayInfo / RenovLine / RenovPkg / RenovTier / RenovHalfMine / ChatItem / TimeCell` 等仅内部使用的类型。

### 4.4 不可达但**故意保留**的防御分支（5 处）

| 位置 | 分支 | 为何保留 |
|---|---|---|
| `utils/scenes-start.ts:240-242` | `sceneQa`：`if (!q) return sceneBlocked(S)` | `buildQA()` 只会产出 `QA_DEFS` 已有的 key，实际不可达 |
| `utils/scenes-renov.ts:273-275` | `sceneRenovCard`：`if (!def) return [{t:"sub",text:""}]` | 仅上划卡场景会进此函数，`findRenovDef` 必然命中 |
| `utils/scenes.ts:99-104` | `buildScene` default 的 `return plainView([])` | `SceneKey` 的 38 个成员已被 28 个 case + 10 个上划卡枚举全覆盖 |
| `utils/render.ts:61` | `buildGuard`：`… : SCREENS[0]` | 装修屏已在函数首行 early-return，`screenIdx` 恒 ≥ 0 |
| `index/index.ts:210` | `render`：`\|\| this.anchorN === undefined` | `onLoad` 已初始化，但删掉就要动可选类型与回顶逻辑 |

删除它们需要引入非空断言或改类型，收益为负；**保留为"防御性兜底"**更稳。

## 5. ⚠️ 重构回归（非死代码，本次未动，需你决策）

### 5.1 `bindtap="onModalMask"` 指向不存在的方法

- 位置：`index/index.wxml:481`（付款确认遮罩层）
- 现状：`index/index.ts` 的 `PageCustom` 与实现里**没有** `onModalMask`。重构前（`9893c902^`）同为悬空引用。
- 影响：点遮罩不会关弹层，只能点「再想想」。功能未崩，但属明确缺陷。
- 两选一：① 删掉该属性（零行为变化，纯清理）；② 补回 `onModalMask() { this.closeModal(); }`（恢复"点遮罩关闭"）。**我倾向 ②**，但这是行为变更，等你点头。

### 5.2 弹层按钮行样式被重构误删

- 位置：`index/index.wxml:514` 使用 `class="sheet-actions"`，但 `index/styles/modal.wxss` 里**已无该规则**（本次重构 `modal.wxss` 删除 350 行时连带删掉，原定义在 `9893c902^` 的 `modal.wxss:75-81`）。
- 影响：「再想想 / 确认支付」两个按钮失去 flex 布局，由并排变成上下堆叠。
- 恢复只需 6 行：

```css
.sheet-actions { display: flex; gap: 20rpx; margin-top: 32rpx; }
.sheet-actions .btn { flex: 1; }
```

### 5.3 另外两个"用了但没定义"的类（重构前就是如此，非本次重构引入）

- `index/index.wxml:33/41/72/80` `.gm-sub`（状态带 / 装修双条的副说明文字）
- `index/index.wxml:342` `.cl-write-b`（清单「写清：」前缀）

两者在 `9893c902^` 中也无定义（`git grep` 可证），属历史遗留缺失。补样式会改变视觉，**未动**。

## 6. 验证记录

```
tsc --noEmit（项目全量）                              → 0 错误
tsc -p <模块级审计配置> --noEmit（含 noUnusedLocals） → 0 错误
vitest run                                           → 20 文件 / 495 用例 全通过（基线同为 495）
  └ house-simulator-scenes.test.ts  45 例（38 屏冒烟：每屏产出非空块、关键分支出口）
  └ house-simulator-handlers.test.ts 41 例（完整流程状态机）
  └ house-simulator-matrix.test.ts   94 例（房源 × 身份 × 贷款方式组合）
残留引用自检（thumbCls/sellerTag/RENOV_ALL/payKind/.intro/opt-tag/gm-loss/steps-top/btn-rust/btn-sm）→ 全部 0
样式类审计 → 删除后 wxss 已无"有定义无消费者"的独立规则（仅剩 `.brow .k` 等后代选择器，属脚本误报）
产物卫生 → 无多余 .js / 临时目录残留（git status 仅 16 个目标文件为 M）
```

## 7. 改动文件清单（16 个，+23 / −106 行）

`index/index.ts`、`index/styles/{base,flow,forms,opts,renov}.wxss`、`utils/{calc,constants,handlers,renov-data,scenes-close,scenes-common,scenes-loan,scenes-money,scenes-nego,scenes-renov}.ts`
