# 装修软装附件上传 + 带看记录日期选择器修复 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 admin 装修合同里的「软装明细附件」从手填链接改为文件上传（Excel/PDF，单文件 ≤100MB），并修复 miniapp 带看记录新增表单日期选择器选不到 31 日的问题。

**Architecture:**
- 任务 1 纯前端改动：后端 `POST /api/v1/files/upload` 已支持 `.xlsx/.xls/.pdf`（`settings.allowed_extensions`），字段 `soft_detail_attachment` 已是 `Text` 列，**无需后端改动、无需数据库迁移、无需 `pnpm gen-api`**。复用 `@/components/common/upload` 的 `FileUploader`（项目附件库已在用同一模式），仅替换编辑态的文本输入框。
- 任务 2 是 miniapp（原生微信小程序）纯前端改动：`<picker mode="multiSelector">` 的列联动必须写在 `bindcolumnchange` 里，而当前页面只绑了 `bindchange`，导致「日」列只在打开表单时按“今天的月份”构建一次，切到更长月份时选不到月末日。把选择器状态计算抽成纯函数模块（同时让 `index.ts` 不再逼近 500 行上限），在 `bindcolumnchange` 中重建日列。

**Tech Stack:** Next.js 16 (App Router) + TS + shadcn/ui + react-hook-form + Zod + vitest｜原生微信小程序 + TypeScript + vitest

---

## 0. 前置确认（开工前需用户拍板）

### 0.1 字段身份

需求写的是「软装清单」，代码库中**不存在**字面量「软装清单」（全仓 grep 0 命中）。装修合同里唯一的「链接录入型」字段是：

| 项 | 值 |
| --- | --- |
| 后端字段 | `soft_detail_attachment`（[`_project_renovation.py`](file:///Users/wither-e/Desktop/profo/backend/models/project/_project_renovation.py#L92)，`Text`，注释「软装明细附件」） |
| 前端标签 | 「软装明细附件」（[`contract-sections.tsx`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L308)） |
| 当前交互 | `<Input type="text" placeholder="请输入软装明细附件链接" />` |

**✅ 已确认（2026-09-20）**：软装清单 = `soft_detail_attachment`，按本计划执行。
**不修改 UI 标签文案**（仍为「软装明细附件」）。

### 0.2 100MB 限制落在哪一层

| 层 | 现状 | 本计划动作 |
| --- | --- | --- |
| 前端 | — | **新增**：仅 Excel/PDF + 单文件 ≤100MB 校验（`validateFile`） |
| Next.js 代理 | `proxyClientMaxBodySize: "500mb"`（[`next.config.ts`](file:///Users/wither-e/Desktop/profo/frontend/next.config.ts#L25)） | 不动 |
| 后端 | `settings.max_upload_size = 500MB`（[`settings.py`](file:///Users/wither-e/Desktop/profo/backend/settings.py#L106)），扩展名白名单已含 `.xlsx/.xls/.pdf` | **不动** |
| Nginx（生产） | `client_max_body_size 100M`（[`profo.nginx.backup`](file:///Users/wither-e/Desktop/profo/profo.nginx.backup#L69)） | **不动**（见 §4 风险 R1） |

即：**✅ 已确认（2026-09-20）100MB 仅前端处理**，与既有「项目附件」功能的做法完全一致（[`attachment-types.ts`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/attachment-types.ts#L102-L113) 也是前端限 100MB、后端 500MB）。后端保持 500MB 全局上限不动。

### 0.3 「移除时同时删除附件」的落点：✅ 已确认采用方案 A（保存时删除）

`StorageBackend.delete_file(key)` 已存在于 [storage.py](file:///Users/wither-e/Desktop/profo/backend/utils/storage.py#L39-L47)，但**生产代码中零调用**（只有 `tests/test_storage.py` 在测），也不存在任何通用文件删除端点。因此「移除时删除附件」需要新增后端逻辑。

**用户已拍板（2026-09-20）：采用 A —— 在 `RenovationService.update_contract` 保存时删除旧文件。**

已否决的方案 B（新增 `DELETE /api/v1/files`，点「移除」立即删）被否决的原因：表单「取消」是 `window.location.reload()`（[`contract-form/index.tsx`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/index.tsx#L178-L181)），「点移除 → 点取消」后 DB 仍存旧 URL，只读态「预览」会变成死链；且删除端点可被用于删除任意已上传文件，需额外做归属校验。

**方案 A 的判定规则**（实现在 `update_contract`，详见 Task 1）：

| 客户端 payload | 落库结果 | 是否删旧文件 |
| --- | --- | --- |
| `soft_detail_attachment=""`（移除） | `""` | ✅ 删旧文件 |
| `soft_detail_attachment="/static/uploads/新文件.pdf"`（替换） | 新 URL | ✅ 删旧文件 |
| `soft_detail_attachment=null` | 不变（Service 的 `value is not None` 过滤会跳过） | ❌ 不删（视为未修改） |
| 不传该字段 | 不变 | ❌ 不删 |

> ⚠️ 已知不覆盖：「上传 A → 上传 B 替换 → A 从未保存过」这种未保存的中间态会留下孤儿文件（需要临时文件 GC 机制），不在本次范围。

### 0.4 测试策略与测试环境（显式声明，避免假成功）

| 范围 | 是否有自动化测试 | 理由 |
| --- | --- | --- |
| Task 1 · 后端（URL 反解存储键 + 保存时清理旧文件） | **有 pytest** | 涉及「旧文件被删 / 新文件不被误删 / 未传字段不动 / 删除失败不阻断保存 / 非法 URL 跳过」五类分支，必须测。文件：`backend/tests/test_renovation_contract_attachment.py`（新建）、`backend/tests/test_storage.py`（追加 `extract_storage_key` 用例）。 |
| Task 2 · 前端（上传组件装配 + 十几行校验） | **无** | 纯 UI 装配，无值得单测的纯逻辑；`renovation/contract-form/` 目录下本就没有测试文件。按仓库「单次使用不抽 util / 不写非必需代码」规则，校验函数内联在 `contract-sections.tsx` 模块作用域，不为测试单开模块。验证 = `pnpm typecheck` + `pnpm lint` + 手动上传验证（Task 2 Step 8）。 |
| Task 3 · miniapp 日期选择器 | **有 vitest**（已完成，commit `fcc5a885`） | 6 个用例，已跑通「失败测试 → 实现 → 通过」。 |

**这是本计划对 writing-plans「每个任务都 TDD」要求的显式豁免与说明，不是遗漏。**

#### 后端测试环境的两个坑（不处理会误判为「测试失败」）

1. **`.env` 的 `DATABASE_URL` 指向 docker 服务名 `db`**，本机无法解析 → 必须覆盖为 `127.0.0.1`；且 conftest 会把库名强制改写为 `profo_test`（启动即 TRUNCATE，不碰开发库 `profo`）。
2. **`.env` 的 `STORAGE_BACKEND=oss`** → 本机跑测试时 `get_storage_backend()` 会去实例化 `OSSStorage`（需要真实 OSS 凭据）。测试必须像 [`test_upload_large_file.py`](file:///Users/wither-e/Desktop/profo/backend/tests/test_upload_large_file.py#L66-L72) 那样用 fixture 强制 `storage_backend="local"` + `upload_dir=tmp_path` 并重置单例，否则是环境敏感测试（该坑在 [2026-09-05 审查报告](file:///Users/wither-e/Desktop/profo/docs/Done/2026-09-05-最佳实践审查报告-commit-f8a2d41e.md) §4.3 已被记录过一次）。

**聚焦跑单个文件**（`pyproject.toml` 的 `addopts` 带 `--cov` 且 `fail_under=82`，单跑一个文件必然因覆盖率不达标而 FAIL，必须加 `--no-cov`）：

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest tests/test_renovation_contract_attachment.py tests/test_storage.py --no-cov -q
```

Expected: 全部 passed（已实测该命令可用：`tests/test_renovation_stage.py` → `8 passed`）。

**提交前的全量校验**：

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest -q && \
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```

> ### 🔴 实测发现（2026-09-20）：`pytest` 在本仓库**永远不可能「全绿」**
>
> 全量实测：`964 passed`，但**覆盖率 68.97% < `fail_under = 82`**，pytest 退出码非 0。这不是本次改动引入的：
> - 本次改动净增 ~70 行代码（`utils/storage.py` 的 `extract_storage_key` 覆盖率 96%），覆盖率是**上升**的（实施者用临时 worktree 对比基线 HEAD~2：68.82% → 68.97%，用例 949 → 964）。新增代码不可能造成 13pp 的下降。
> - [`.github/workflows/lint.yml`](file:///Users/wither-e/Desktop/profo/.github/workflows/lint.yml) **只跑 ruff，不跑 pytest**，所以这道门槛从未被 CI 拦截过。
>
> **结论**：AGENTS.md 的「`pytest` 全绿」在本环境无法满足，属既有状态（`utils/jwt_validator.py` 0%、`utils/security_logger.py` 60% 等大量既有未覆盖）。本次验收标准改为「**全量用例 0 失败 + ruff 全绿**」，覆盖率门槛问题另行处理，不在本次范围。

---

## 1. 文件结构

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `backend/utils/storage.py` | 改 | 新增模块级 `extract_storage_key(url)`：从访问 URL 反解存储键（与 `upload_file` 互为逆运算），带安全校验 |
| `backend/services/projects/renovation.py` | 改 | `update_contract` 在附件被移除/替换时删除旧物理文件（best-effort）；新增模块级 `_delete_storage_file` |
| `backend/tests/test_storage.py` | 改 | 追加 `extract_storage_key` 用例（local/OSS URL、query 剥离、外部域名拒绝、路径穿越拒绝） |
| `backend/tests/test_renovation_contract_attachment.py` | 新建 | `update_contract` 附件清理的 5 类分支测试 |
| `frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx` | 改 | 软装明细附件：编辑态改为 `FileUploader`，只读态收敛为「预览」链接 |
| `miniapp/pages/viewing/utils/datetime-picker.ts` | 新建 | 日期时间选择器（年/月/日/时/分）纯函数状态计算：构建、联动归一化 |
| `miniapp/pages/viewing/detail/index/index.ts` | 改 | 删除内联的 DT_* 常量与日列计算，改为调用上者；新增 `onDtColumnChange` |
| `miniapp/pages/viewing/detail/index/index.wxml` | 改 | picker 增加 `bindcolumnchange="onDtColumnChange"` |
| `miniapp/test-utils/utils/datetime-picker.test.ts` | 新建 | 日列随年月联动、月末夹紧、闰年天数 |

**不动的文件**（明确排除，避免误改）：`backend/routers/**`（不新增删除端点）、`backend/settings.py`、`backend/migrations/**`、`backend/models/project/_project_renovation.py`、`backend/schemas/project/renovation.py`、`frontend/src/lib/api-types.d.ts`、`miniapp/types/api-types.d.ts`、`contract-form/schema.ts`、`actions/renovation.ts`、`contract-form/index.tsx`。

> 关于 `soft_detail_attachment` 的 `max_length=500`（Pydantic / Zod）：DB 列已是 `text`，上传后 URL 形如 `/static/uploads/20260920_ab12cd34.pdf`（OSS 场景约 80~100 字符），远小于 500，**本次不动**。

---

## Task 1: 后端 · 附件 URL 反解存储键 + 保存时清理旧附件文件（✅ 已完成，commit `379f544a`）

**Files:**
- Modify: `backend/utils/storage.py`
- Modify: `backend/services/projects/renovation.py`
- Test: `backend/tests/test_storage.py`（追加）
- Test: `backend/tests/test_renovation_contract_attachment.py`（新建）

**背景（实现者必读）：**
- 判定规则见 §0.3 表格。核心：`update_contract` 的写入循环带 `value is not None` 过滤，所以 `null` 会被跳过（视为未修改），而 `""` 会被正常写入 → 用「旧值 ≠ 新值」作为删除条件即可同时覆盖「移除」与「替换」。
- 删除必须放在 `self.db.commit()` **之后**：先保证保存成功，再清理文件；删除失败只记日志，绝不影响保存结果。
- 测试环境有两个坑（`.env` 的 `DATABASE_URL` 指向 docker 主机 `db`、`STORAGE_BACKEND=oss`），命令与 fixture 写法见 §0.4，**务必先读**，否则会把环境问题误判成代码问题。
- ruff `line-length = 120`、`select = ["ALL"]`（忽略清单见 `backend/pyproject.toml`），新代码需要完整类型注解 + docstring。

- [ ] **Step 1: 写失败测试 —— `extract_storage_key`**

在 [`test_storage.py`](file:///Users/wither-e/Desktop/profo/backend/tests/test_storage.py) 的 import 区把 `from utils.storage import LocalStorage, OSSStorage, StorageBackend, get_storage_backend` 改为：

```python
from utils.storage import (
    LocalStorage,
    OSSStorage,
    StorageBackend,
    extract_storage_key,
    get_storage_backend,
)
```

并在**文件末尾**追加：

```python
class TestExtractStorageKey:
    """从访问 URL 反解存储键（与 upload_file 互为逆运算）."""

    def test_local_url(self) -> None:
        """local 模式 /static/uploads/{key} → {key}."""
        assert extract_storage_key("/static/uploads/20260722_abc.pdf") == "20260722_abc.pdf"

    def test_local_url_strips_query_string(self) -> None:
        """剥离 query string（图片处理参数 / 签名 URL）."""
        assert extract_storage_key("/static/uploads/a.pdf?x-oss-process=style/profo") == "a.pdf"
        assert extract_storage_key("/static/uploads/a.pdf?v=1#frag") == "a.pdf"

    def test_local_url_nested_key(self) -> None:
        """支持子目录 key（如 thumbs/xxx.webp）."""
        assert extract_storage_key("/static/uploads/thumbs/abc.webp") == "thumbs/abc.webp"

    def test_oss_url(self, monkeypatch: pytest.MonkeyPatch) -> None:
        """oss 模式 {oss_public_base_url}/{key} → {key}."""
        monkeypatch.setattr(storage_module.settings, "oss_public_base_url", "https://cdn.example.com")
        assert extract_storage_key("https://cdn.example.com/20260722_abc.pdf") == "20260722_abc.pdf"

    def test_oss_url_requires_configured_base(self, monkeypatch: pytest.MonkeyPatch) -> None:
        """oss_public_base_url 未配置 → 无法反解，返回 None."""
        monkeypatch.setattr(storage_module.settings, "oss_public_base_url", None)
        assert extract_storage_key("https://cdn.example.com/a.pdf") is None

    def test_rejects_foreign_host(self, monkeypatch: pytest.MonkeyPatch) -> None:
        """非白名单 hostname 一律拒绝（含前缀绕过写法）."""
        monkeypatch.setattr(storage_module.settings, "oss_public_base_url", "https://cdn.example.com")
        assert extract_storage_key("https://cdn.example.com.evil.com/a.pdf") is None
        assert extract_storage_key("https://cdn.example.com@evil.com/a.pdf") is None
        assert extract_storage_key("https://evil.com/a.pdf") is None

    def test_rejects_path_traversal(self) -> None:
        """含 .. 的 key 一律拒绝，避免删除任意路径."""
        assert extract_storage_key("/static/uploads/../../etc/passwd") is None
        assert extract_storage_key("/static/uploads/a/../../b.pdf") is None

    def test_rejects_backslash(self) -> None:
        """含反斜杠的 key 一律拒绝（Windows 路径分隔符）."""
        assert extract_storage_key("/static/uploads/..\\a.pdf") is None

    def test_rejects_empty_and_plain_name(self) -> None:
        """空值 / 非 URL 形态的裸文件名 → None."""
        assert extract_storage_key(None) is None
        assert extract_storage_key("") is None
        assert extract_storage_key("just-a-name.pdf") is None
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest tests/test_storage.py --no-cov -q
```

Expected: FAIL — `ImportError: cannot import name 'extract_storage_key' from 'utils.storage'`。

- [ ] **Step 3: 实现 `extract_storage_key`**

在 [`storage.py`](file:///Users/wither-e/Desktop/profo/backend/utils/storage.py) 顶部 import 区把 `import logging` 改为 `import logging` + `import re`（保持 stdlib 字母序，即 `logging` 在前、`re` 在后、`shutil` 之前）：

```python
import logging
import re
import shutil
```

在 `_storage_backend: StorageBackend | None = None` **之前**插入：

```python
# 本地静态文件 URL 前缀（与 LocalStorage.upload_file 的返回值约定一致）
_STATIC_UPLOADS_PREFIX = "/static/uploads/"

# 存储键白名单：首字符为字母/数字，其余允许字母/数字/点/下划线/连字符/斜杠。
# 借此排除 .. 与反斜杠等路径穿越写法，避免 delete_file 被用于删除任意路径。
_STORAGE_KEY_PATTERN = re.compile(r"^[A-Za-z0-9][A-Za-z0-9._/-]*$")


def extract_storage_key(url: str | None) -> str | None:
    """从文件访问 URL 反解出存储键（即 ``upload_file`` 的 ``key``）.

    支持两种形态（与 ``upload_file`` 的返回值互为逆运算）：
    - local: ``/static/uploads/{key}``
    - oss:   ``{settings.oss_public_base_url}/{key}``

    无法识别、或 key 不安全（含 ``..`` / 反斜杠 / 非法字符）时返回 None，
    调用方据此跳过删除，避免误删任意路径。

    Examples:
        >>> extract_storage_key("/static/uploads/20260722_abc.pdf")
        '20260722_abc.pdf'

    """
    if not url:
        return None

    # 剥离 query string / fragment（图片处理参数、签名 URL 参数）
    candidate = url.strip().split("?", 1)[0].split("#", 1)[0]

    if candidate.startswith(_STATIC_UPLOADS_PREFIX):
        key = candidate[len(_STATIC_UPLOADS_PREFIX) :]
    else:
        base = (settings.oss_public_base_url or "").rstrip("/")
        # 必须匹配 `{base}/` 前缀：可挡住 https://cdn.example.com.evil.com 这类前缀绕过
        if not base or not candidate.startswith(f"{base}/"):
            return None
        key = candidate[len(base) + 1 :]

    if not key or ".." in key or "\\" in key or not _STORAGE_KEY_PATTERN.match(key):
        return None
    return key
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest tests/test_storage.py --no-cov -q
```

Expected: PASS（既有 storage 用例 + 新增 9 个用例全绿）。

- [ ] **Step 5: 写失败测试 —— `update_contract` 附件清理**

新建 `backend/tests/test_renovation_contract_attachment.py`：

```python
"""RenovationService.update_contract 软装明细附件的物理文件清理测试.

回归需求：admin 装修合同的「软装明细附件」由手填链接改为文件上传后，
「移除 / 替换」附件时必须同时删除存储中的旧文件，避免孤儿文件堆积（方案 A：保存时删除）。

覆盖 6 类分支：
1. 旧 URL → 空串（移除）→ 删旧文件
2. 旧 URL → 新 URL（替换）→ 删旧文件，新文件保留
3. 未传 soft_detail_attachment → 不动文件
4. 传 null → 视为未修改，不删文件
5. 删除失败（storage 抛异常）→ 保存仍成功，字段已更新
6. 无法反解的外部域名 URL → 跳过删除且不报错
"""

import uuid
from collections.abc import Generator
from decimal import Decimal
from pathlib import Path
from typing import Any

import pytest
from sqlalchemy.orm import Session

from models import Project, ProjectRenovation
from models.common import ProjectStatus
from schemas.project.renovation import RenovationContractUpdate
from services.projects.renovation import RenovationService
from utils import storage as storage_module


@pytest.fixture(autouse=True)
def _local_storage(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> Generator[None, None, None]:
    """强制 local 存储 + 临时 upload_dir.

    .env 默认 STORAGE_BACKEND=oss，不覆盖会去实例化 OSSStorage（需要真实凭据）；
    测试结束需重置单例，避免泄漏到后续测试。
    """
    storage_module._storage_backend = None
    monkeypatch.setattr(storage_module.settings, "storage_backend", "local")
    monkeypatch.setattr(storage_module.settings, "upload_dir", str(tmp_path))
    yield
    storage_module._storage_backend = None


def _make_project(session: Session, *, project_id: str) -> Project:
    """创建并持久化处于装修中的项目."""
    project = Project(
        id=uuid.uuid4(),
        name=f"测试项目-{project_id}",
        community_name="测试小区",
        address="测试地址",
        status=ProjectStatus.RENOVATING,
        is_deleted=False,
    )
    session.add(project)
    session.commit()
    session.refresh(project)
    return project


def _make_renovation(session: Session, *, project_id: uuid.UUID, attachment: str | None) -> ProjectRenovation:
    """创建并持久化带软装明细附件的装修记录."""
    renovation = ProjectRenovation(
        id=uuid.uuid4(),
        project_id=project_id,
        soft_detail_attachment=attachment,
        is_deleted=False,
    )
    session.add(renovation)
    session.commit()
    session.refresh(renovation)
    return renovation


def _attachment_of(session: Session, project_id: uuid.UUID) -> str | None:
    """读取当前落库的软装明细附件."""
    row = session.query(ProjectRenovation).filter(ProjectRenovation.project_id == project_id).first()
    return row.soft_detail_attachment if row else None


class TestUpdateContractAttachmentCleanup:
    """update_contract 在附件被移除/替换时删除旧物理文件."""

    def test_clear_attachment_deletes_old_file(self, seeded_db: dict[str, Any], tmp_path: Path) -> None:
        """附件由旧 URL 变为空串（移除）→ 删除旧文件."""
        session = seeded_db["session"]
        old_key = "20260920_oldattach.pdf"
        (tmp_path / old_key).write_bytes(b"old content")
        project = _make_project(session, project_id="proj-attach-clear")
        _make_renovation(session, project_id=project.id, attachment=f"/static/uploads/{old_key}")

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(soft_detail_attachment=""),
        )

        assert _attachment_of(session, project.id) == ""
        assert not (tmp_path / old_key).exists()

    def test_replace_attachment_deletes_old_file(self, seeded_db: dict[str, Any], tmp_path: Path) -> None:
        """附件由旧 URL 变为新 URL（替换）→ 删旧文件、保留新文件."""
        session = seeded_db["session"]
        old_key = "20260920_oldattach.pdf"
        new_key = "20260920_newattach.xlsx"
        (tmp_path / old_key).write_bytes(b"old content")
        (tmp_path / new_key).write_bytes(b"new content")
        project = _make_project(session, project_id="proj-attach-replace")
        _make_renovation(session, project_id=project.id, attachment=f"/static/uploads/{old_key}")

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(soft_detail_attachment=f"/static/uploads/{new_key}"),
        )

        assert _attachment_of(session, project.id) == f"/static/uploads/{new_key}"
        assert not (tmp_path / old_key).exists()
        assert (tmp_path / new_key).exists()

    def test_omitted_field_keeps_old_file(self, seeded_db: dict[str, Any], tmp_path: Path) -> None:
        """未传 soft_detail_attachment → 字段与文件都不变."""
        session = seeded_db["session"]
        old_key = "20260920_oldattach.pdf"
        (tmp_path / old_key).write_bytes(b"old content")
        project = _make_project(session, project_id="proj-attach-omitted")
        _make_renovation(session, project_id=project.id, attachment=f"/static/uploads/{old_key}")

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(hard_contract_amount=Decimal("100000")),
        )

        assert _attachment_of(session, project.id) == f"/static/uploads/{old_key}"
        assert (tmp_path / old_key).exists()

    def test_null_attachment_keeps_old_file(self, seeded_db: dict[str, Any], tmp_path: Path) -> None:
        """显式传 null → 被 value is not None 过滤跳过，视为未修改."""
        session = seeded_db["session"]
        old_key = "20260920_oldattach.pdf"
        (tmp_path / old_key).write_bytes(b"old content")
        project = _make_project(session, project_id="proj-attach-null")
        _make_renovation(session, project_id=project.id, attachment=f"/static/uploads/{old_key}")

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(soft_detail_attachment=None),
        )

        assert _attachment_of(session, project.id) == f"/static/uploads/{old_key}"
        assert (tmp_path / old_key).exists()

    def test_delete_failure_does_not_break_save(
        self,
        seeded_db: dict[str, Any],
        tmp_path: Path,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """storage 删除抛异常 → 保存仍成功，字段已更新（best-effort 清理）."""
        session = seeded_db["session"]
        old_key = "20260920_oldattach.pdf"
        (tmp_path / old_key).write_bytes(b"old content")
        project = _make_project(session, project_id="proj-attach-delete-fail")
        _make_renovation(session, project_id=project.id, attachment=f"/static/uploads/{old_key}")

        def _raise(key: str) -> bool:
            msg = "OSS 删除失败"
            raise OSError(msg)

        monkeypatch.setattr(
            "services.projects.renovation.get_storage_backend",
            lambda: type("_S", (), {"delete_file": staticmethod(_raise)})(),
        )

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(soft_detail_attachment=""),
        )

        assert _attachment_of(session, project.id) == ""
        assert (tmp_path / old_key).exists()

    def test_unresolvable_url_skips_deletion(self, seeded_db: dict[str, Any]) -> None:
        """附件为外部域名 URL → 无法反解存储键，跳过删除且不报错."""
        session = seeded_db["session"]
        project = _make_project(session, project_id="proj-attach-external")
        _make_renovation(session, project_id=project.id, attachment="https://example.com/old.pdf")

        RenovationService(db=session).update_contract(
            project.id,
            RenovationContractUpdate(soft_detail_attachment=""),
        )

        assert _attachment_of(session, project.id) == ""
```

- [ ] **Step 6: 跑测试确认失败**

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest tests/test_renovation_contract_attachment.py --no-cov -q
```

Expected: **3 failed / 3 passed**（实测结果，与原稿预测的「2 failed / 4 passed」略有差异，见下方修正说明）。

> **实现期修正（2026-09-20，commit `379f544a`）**：
> 1. 失败的是 `test_clear_attachment_deletes_old_file`、`test_replace_attachment_deletes_old_file`（断言「旧文件已删」失败），以及 `test_delete_failure_does_not_break_save` —— 它在本 Step 会因 `services.projects.renovation` 尚未导入 `get_storage_backend`、monkeypatch 阶段直接 `AttributeError` 而失败（Step 7 后转绿）。原稿预测的「4 个护栏用例全部意外通过」不准确。
> 2. 原稿给的测试代码有 5 处 ruff 报错，已逐条修正：`Decimal("100000")` → `Decimal(100000)`（FURB157）；docstring 首词需大写，`storage 删除…` → `存储删除…`、`local 模式` → `本地模式`、`oss 模式` → `OSS 模式`（D403）；`lambda: type("_S", (), {...})()` 拆为局部变量 `failing_storage` + `lambda: failing_storage`（PLW0108）。**断言与语义完全未变。**

- [ ] **Step 7: 实现 Service 侧清理**

7a. [`renovation.py`](file:///Users/wither-e/Desktop/profo/backend/services/projects/renovation.py#L7-L17) 的 import 区改为：

```python
import logging
import uuid
from datetime import datetime, timezone
from typing import Any

from sqlalchemy.orm import Session
from sqlalchemy.orm.attributes import flag_modified

from models import Project, ProjectRenovation, RenovationPhoto
from models.common import ProjectStatus, RenovationStage
from schemas.project.renovation import RenovationContractUpdate, RenovationUpdate
from services.system.exceptions import BusinessLogicError, ResourceNotFoundError
from utils.storage import extract_storage_key, get_storage_backend

logger = logging.getLogger(__name__)
```

7b. 在 `_RENOVATION_ALLOWED_FIELDS` 集合定义之后、`class RenovationService:` 之前插入：

```python
def _delete_storage_file(url: str) -> None:
    """删除存储中的物理文件（best-effort：解析失败或删除失败仅记日志，不抛异常）.

    用于软装明细附件被移除/替换后清理旧文件，避免孤儿文件堆积。
    """
    key = extract_storage_key(url)
    if key is None:
        logger.warning("装修合同：无法从附件 URL 反解存储键，跳过删除: %s", url)
        return
    try:
        get_storage_backend().delete_file(key)
    except Exception:
        logger.exception("装修合同：删除旧附件文件失败（不影响保存结果）: %s", key)
```

7c. 把 [`update_contract`](file:///Users/wither-e/Desktop/profo/backend/services/projects/renovation.py#L411-L423) 的实现体从 `renovation = self._get_or_create_renovation(project_id)` 起替换为：

```python
        renovation = self._get_or_create_renovation(project_id)

        # 更新字段（使用白名单过滤，防止设置敏感字段）
        update_data = contract_data.model_dump(exclude_unset=True)
        # 附件被移除/替换前先记下旧值，保存成功后删除其物理文件（方案 A：保存时清理）
        # 注：value is not None 过滤会让显式 null 被跳过，故此处「旧值 != 新值」不会误判
        old_attachment = (
            renovation.soft_detail_attachment if "soft_detail_attachment" in update_data else None
        )
        for field, value in update_data.items():
            if field in _RENOVATION_ALLOWED_FIELDS and value is not None:
                setattr(renovation, field, value)

        renovation.updated_at = datetime.now(timezone.utc)
        self.db.commit()
        self.db.refresh(renovation)

        # 旧附件已不再被引用 → 删除物理文件（失败只记日志，保存结果不受影响）
        if old_attachment and old_attachment != renovation.soft_detail_attachment:
            _delete_storage_file(old_attachment)

        return renovation
```

- [ ] **Step 8: 跑测试确认通过**

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest tests/test_renovation_contract_attachment.py tests/test_storage.py --no-cov -q
```

Expected: PASS（6 + 既有 storage 用例全绿）。

- [ ] **Step 9: 全量测试 + ruff**

```bash
cd /Users/wither-e/Desktop/profo/backend && \
DATABASE_URL='postgresql+psycopg://profo:OIn9DECrC3E5CXXqGz6V41mc@127.0.0.1:5432/profo_test' \
.venv/bin/python -m pytest -q && \
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```

Expected: 全量 **964 passed、0 failed**；`ruff check .` 与 `ruff format --check .` 均无输出。若 `ruff format --check` 报格式差异，运行 `.venv/bin/python -m ruff format .` 后重跑。

> ⚠️ 覆盖率门槛会失败（68.97% < 82%），属既有状态，原因与处置见 §0.4 的实测发现。**不要为了凑覆盖率写无意义测试。**

- [ ] **Step 10: 提交**

```bash
git add backend/utils/storage.py backend/services/projects/renovation.py \
        backend/tests/test_storage.py backend/tests/test_renovation_contract_attachment.py
git commit -m "feat(renovation): 软装明细附件被移除/替换时删除旧物理文件"
```

---

## Task 2: 前端 · admin 装修合同「软装明细附件」改为文件上传（✅ 已完成，commit `6548d7e3`）

**Files:**
- Modify: `frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx`（394 行）

**背景（实现者必读）：**
- `FileUploader` 的 `onUploadComplete(response, file)` 里 `response.url` 已被 `parseUploadResponse` 经 `getFileUrl()` 归一化为**相对路径**（如 `/static/uploads/20260920_ab12cd34.pdf`），OSS 场景下指向外部域名则保留绝对 URL。两种形态都能通过只读态的 `isValidUrl()`（[`validators.ts`](file:///Users/wither-e/Desktop/profo/frontend/src/lib/validators.ts#L10-L18) 对 `/` 开头的相对路径返回 `true`），所以「预览」链接在本地与 OSS 两种存储后端下都可用。
- 上传端点权限 `LeadUploadPhotoPermDep` = `property:upload` **或** `lead:upload_photo`；内置角色 `admin`（全权限）、`operator`、`user` 三者都持有其中之一（[`_seeds.py`](file:///Users/wither-e/Desktop/profo/backend/migrations/_seeds.py#L391-L437)），装修编辑所需权限 `project:sales:manage_team` 仅 `admin/operator` 持有，是上传权限的子集，**不存在权限缺口**。
- `FileUploader` 内部维护自己的文件列表（含进度/重试/删除），与 react-hook-form 的值是两套状态：表单值只存最后一个上传成功的 URL。这是刻意选择——保留进度与重试能力，代价是连续上传两次时列表会显示两条记录（仅视觉冗余，最终保存的是最后一条，功能正确）。

- [ ] **Step 1: 加常量与校验函数**

在 [`contract-sections.tsx`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L33-L34) 的 `ContractSectionsProps` 接口之后插入：

```tsx
/** 软装明细附件：仅 Excel / PDF，单文件最大 100MB（与 attachment-types 文档类限额一致） */
const SOFT_LIST_EXTENSIONS = [".xlsx", ".xls", ".pdf"];
const SOFT_LIST_MIME_TYPES = [
  ...ALLOWED_FILE_TYPES.excel.mimeTypes,
  ...ALLOWED_FILE_TYPES.pdf.mimeTypes,
];
const SOFT_LIST_MAX_SIZE = MAX_FILE_SIZE_BY_TYPE.excel;

/** 软装明细附件上传校验：扩展名白名单 + 100MB 上限；返回错误文案，通过返回 null. */
function validateSoftListFile(file: File): string | null {
  const ext = file.name.toLowerCase().slice(file.name.lastIndexOf("."));
  if (!SOFT_LIST_EXTENSIONS.includes(ext)) {
    return "仅支持 Excel（.xlsx/.xls）或 PDF 文件";
  }
  if (file.size > SOFT_LIST_MAX_SIZE) {
    return `文件大小超过限制（最大 ${formatFileSize(SOFT_LIST_MAX_SIZE)}）`;
  }
  return null;
}
```

- [ ] **Step 2: 补 import，删孤儿 import**

改文件头（[当前 1-22 行](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L1-L22)）：

`Input` 在本文件只有软装明细附件那一处用到（第 309 行），替换后必须删除该 import（AGENTS.md：孤儿 import 必须清理）。新增 3 个 import：

```tsx
import { FileUploader } from "@/components/common/upload";
import { formatFileSize } from "@/lib/formatters";
import { ALLOWED_FILE_TYPES, MAX_FILE_SIZE_BY_TYPE } from "../../../attachment-types";
```

其中 `import { Input } from "@/components/ui/input";`（原第 6 行）**删除**。`TextInputField`、`Textarea`、`Select*` 仍在用，保留。

> **实现期修正（2026-09-20，commit `6548d7e3`）**：本 Step 只点出了 `Input` 会变孤儿，**漏了 `import { isValidUrl } from "@/lib/validators"`** —— 它在 Step 4 删掉 `isValidUrl(attachment) && ...` 后同样只剩零引用（该 import 在本文件仅此一处使用），不删会被 eslint `--max-warnings 0` 判错。实施时已一并删除。**教训：本 Step 应改为「替换后跑一次 `pnpm lint` 找出全部孤儿 import」，而不是靠人工列举。**

- [ ] **Step 3: 把 `attachment` 提前到函数顶部**

[`DecorationCostSection`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L234-L237) 当前在只读分支内定义 `attachment`，编辑态也要用，提到 `if (!isEditing)` 之前：

```tsx
export function DecorationCostSection({ values, setValue, isEditing }: ContractSectionsProps) {
  const attachment = values.soft_detail_attachment?.trim();
  // 只读态：金额统一「N.N 万元」；软装明细附件全宽 + 预览 mini-link
  if (!isEditing) {
```

同时删除只读分支里原有的那一行 `const attachment = values.soft_detail_attachment?.trim();`。

- [ ] **Step 4: 只读态收敛为「预览」链接**

值现在是机器生成的存储路径（如 `/static/uploads/20260920_ab12cd34.pdf`），原始文件名未持久化，展示该路径对用户无意义。把 [第 247-256 行](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L247-L256) 替换为：

```tsx
          <InfoItem k="软装明细附件" full>
            {attachment ? (
              <MiniLink href={attachment}>预览</MiniLink>
            ) : (
              <span className="text-[14.5px] font-[400] text-dove">-</span>
            )}
          </InfoItem>
```

> 说明：这一步超出「链接→上传」的最小改动范围，理由是改动后展示原始 URL 已失去信息价值。若希望保留原样（显示 URL 文本 + 预览），跳过本 Step 即可，不影响其余步骤。

- [ ] **Step 5: 编辑态换成上传组件**

把 [第 306-316 行](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx#L306-L316) 整块替换为：

```tsx
        {/* 软装明细附件：上传 Excel/PDF（≤100MB），成功后写入附件 URL */}
        <div className="col-span-3 space-y-2">
          <Label className="text-xs font-medium text-muted-foreground">软装明细附件</Label>
          <FileUploader
            options={{
              allowedTypes: SOFT_LIST_MIME_TYPES,
              multiple: false,
              validateFile: validateSoftListFile,
            }}
            onUploadComplete={(response) => {
              if (response.url) {
                setValue("soft_detail_attachment", response.url, { shouldDirty: true });
              }
            }}
            title="点击或拖拽文件到此处上传"
            description="支持 Excel（.xlsx/.xls）、PDF 格式，单文件最大 100MB"
            className="space-y-2"
          />
          {attachment && (
            <div className="flex items-center gap-3">
              <MiniLink href={attachment}>预览已保存附件</MiniLink>
              <button
                type="button"
                onClick={() => setValue("soft_detail_attachment", "", { shouldDirty: true })}
                className="text-[13px] font-[430] text-error transition-colors hover:underline"
              >
                移除
              </button>
            </div>
          )}
        </div>
```

> 「移除」按钮是必需的：原文本输入框可清空，若只留上传组件就没有任何清除途径（功能回退）。后端 `RenovationContractUpdate.soft_detail_attachment: str | None` 接受空串，`RenovationService.update_contract` 的白名单写入按 `value is not None` 过滤，空串会被正常写入，因此清空链路可用。
> **「移除」只清空表单值，物理文件在点「保存」时由 Task 1 的 Service 逻辑删除**（方案 A）。这是刻意设计：点「移除」后若点「取消」，表单会 `window.location.reload()` 丢弃改动，DB 仍存旧 URL，此时文件必须还在，否则只读态「预览」会变成死链。前端不需要任何删除调用。

- [ ] **Step 6: 类型与 lint 校验**

```bash
cd /Users/wither-e/Desktop/profo/frontend && pnpm typecheck && pnpm lint
```

Expected: 两条命令均无输出（`tsc --noEmit` 零错、eslint `--max-warnings 0` 通过）。

- [ ] **Step 7: 手动验证上传链路**

前提：后端与前端以调试模式运行（`uvicorn main:app --reload --host 0.0.0.0 --port 8000`、`pnpm dev`），**不要**启 docker。

1. 打开 `http://localhost:3000/admin/projects/{任一项目}/renovation`（或项目管理详情 → 装修节点 → 装修合同信息）。
2. 点「编辑」→ 装修费用区出现虚线拖拽上传框。
3. 传一个 `.txt` → 期望 toast：`xxx.txt: 仅支持 Excel（.xlsx/.xls）或 PDF 文件`，且请求未发出。
4. 传一个 `.pdf` → 期望出现进度条 → 成功后上传框下方出现「预览已保存附件」。
5. 点「保存」→ toast「装修合同信息已保存」；刷新页面 → 只读态显示「软装明细附件：预览」。
6. 再点「编辑」→ 点「移除」→ 保存 → 刷新 → 只读态显示 `-`。
7. 后端 `curl` 复核落库值：

```bash
curl -s http://127.0.0.1:8000/api/v1/projects/{project_id}/renovation/contract -H "Authorization: Bearer {token}" | python3 -c "import sys,json;print(json.load(sys.stdin)['soft_detail_attachment'])"
```

Expected: 形如 `/static/uploads/20260920_xxxxxxxx.pdf` 的路径（OSS 部署时为绝对 URL）。

> ### 实测记录（2026-09-20，浏览器自动化，仅只读/拒绝路径）
>
> 项目 `8f758be0-382b-46e3-bbdd-dc4b1debc559`（保德路241弄，renovating），前后端调试进程均在运行：
>
> | # | 检查项 | 结果 |
> | --- | --- | --- |
> | 1 | 页面正常渲染、无 error overlay、「装修费用」区正常 | ✅ 通过 |
> | 2 | 编辑态出现虚线拖拽上传框（`border-style: dashed`），原文本输入框已消失 | ✅ 通过 |
> | 3 | 说明文案 = 「支持 Excel（.xlsx/.xls）、PDF 格式，单文件最大 100MB」 | ✅ 通过 |
> | 4 | `accept` = `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel,application/pdf` | ✅ 通过 |
> | 5 | `multiple = false` | ✅ 通过 |
> | 6 | 选 `.txt` → toast「xxx.txt: 仅支持 Excel（.xlsx/.xls）或 PDF 文件」，且 **Network 中 `/api/v1/files/upload` 命中 0 次**、input 已清空 | ✅ 通过 |
> | 7 | 拒绝路径未产生新的 Console error | ✅ 通过 |
>
> **⚠️ 未验证（刻意跳过）**：本 Step 上面手动清单里的第 4-7 条（真实上传 `.pdf` → 进度条 → 保存 → 只读态「预览」→ 移除 → 保存 → 文件与字段清空 → curl 复核落库值）**全部没有执行**。原因：本机 `.env` 为 `STORAGE_BACKEND=oss`，真实上传会写入生产 OSS 桶（`profo`），故未在未获授权的情况下写入。**这一整条链路需要人工验一次。**
>
> 相应的覆盖缺口：`OSSStorage.delete_file` 的**组合路径**未被端到端跑过（其自身有既有单测 `test_delete_file_calls_delete_object`，`extract_storage_key` 的 OSS URL 形态有单测；Service 层的删除编排由 6 个 pytest 用例在 LocalStorage 上覆盖）。

- [ ] **Step 8: 提交**

```bash
git add frontend/src/app/\(main\)/admin/projects/_components/project-detail/views/renovation/contract-form/contract-sections.tsx
git commit -m "feat(renovation): 软装明细附件由手填链接改为 Excel/PDF 上传（≤100MB）"
```

---

## Task 3: miniapp 带看记录日期选择器补 31 日（✅ 已完成，commit `fcc5a885`）

**Files:**
- Create: `miniapp/pages/viewing/utils/datetime-picker.ts`
- Modify: `miniapp/pages/viewing/detail/index/index.ts`（497 行 → 约 460 行）
- Modify: `miniapp/pages/viewing/detail/index/index.wxml`
- Test: `miniapp/test-utils/utils/datetime-picker.test.ts`

**根因（实现者必读，已交叉验证）：**
- `daysInMonth()` 本身正确（`new Date(year, month, 0).getDate()`，month 为 1-based，`daysInMonth(2026, 8) === 31`）。
- 真正原因：微信 `multiSelector` 的**列联动必须写在 `bindcolumnchange`**，而 [`index.wxml` 第 140-145 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.wxml#L140-L145) 只绑了 `bindchange`。`bindchange` 只在点「确定」时触发，所以滚动月份列时「日」列**不会重建**，一直是打开表单时那份（今天 2026-09-20 → 9 月 30 天 → 日列只有 1~30 日）。切到 8 月后物理上滚不到 31 日；即便点确定，[`onDtChange` 第 351 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L346-L359) 的 `Math.min(v[2], dim - 1)` 也只会把结果夹成 8 月 30 日。
- 仓库内已有正确写法可对照：[`pages/analysis/index/index.wxml` 第 29 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/analysis/index/index.wxml#L24-L31) 绑了 `bindcolumnchange="onPickerColumnChange"`，对应处理在 [`index.ts` 第 409-420 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/analysis/index/index.ts#L409-L420)。
- 后端无日粒度限制：`SalesRecordCreate.record_date: datetime`（[`sales.py`](file:///Users/wither-e/Desktop/profo/backend/schemas/project/sales.py#L58)）只做「无时区则补 Asia/Shanghai」，`2026-08-31T10:00:00` 合法。**后端零改动。**

- [ ] **Step 1: 写失败测试**

新建 `miniapp/test-utils/utils/datetime-picker.test.ts`：

```ts
/**
 * 日期时间选择器状态计算：日列必须随年/月联动重建，
 * 否则「打开选择器时所在月份」天数不足时选不到月末
 * （回归：9 月打开选择器 → 切到 8 月 → 日列仍只有 1~30 日，选不到 31 日）.
 */
import { describe, expect, it } from "vitest";
import { buildPickerState, normalizePicker } from "../../pages/viewing/utils/datetime-picker";

/** 取「日」列文案. */
function dayColumn(state: { dtRange: string[][] }): string[] {
  return state.dtRange[2];
}

describe("buildPickerState", () => {
  it("按给定时间构建五列并定位默认选中项", () => {
    const state = buildPickerState(new Date(2026, 8, 20, 10, 30));
    expect(state.dtYearStart).toBe(2025);
    expect(state.dtRange[0]).toEqual(["2025年", "2026年", "2027年"]);
    expect(dayColumn(state)).toHaveLength(30); // 2026-09 共 30 天
    expect(dayColumn(state)[29]).toBe("30日");
    expect(state.dtValue).toEqual([1, 8, 19, 10, 30]);
    expect(state.dtText).toBe("2026-09-20 10:30");
  });

  it("2 月天数按闰年计算", () => {
    expect(dayColumn(buildPickerState(new Date(2028, 1, 10, 8, 0)))).toHaveLength(29);
    expect(dayColumn(buildPickerState(new Date(2027, 1, 10, 8, 0)))).toHaveLength(28);
  });
});

describe("normalizePicker", () => {
  // 打开表单时的状态：今天 2026-09-20（9 月 30 天）
  const base = buildPickerState(new Date(2026, 8, 20, 10, 30));

  it("月列切到 31 天月份后日列补齐到 31 日（回归：8 月选不到 31 号）", () => {
    const picked = [...base.dtValue];
    picked[1] = 7; // 8月
    const next = normalizePicker(base, picked);
    expect(dayColumn(next)).toHaveLength(31);
    expect(dayColumn(next)[30]).toBe("31日");
    expect(next.dtValue).toEqual([1, 7, 19, 10, 30]);
    expect(next.dtText).toBe("2026-08-20 10:30");
  });

  it("31 日选中项切到 30 天月份后夹紧到月末", () => {
    const aug = normalizePicker(base, [1, 7, 30, 9, 5]); // 2026-08-31 09:05
    expect(aug.dtText).toBe("2026-08-31 09:05");

    const picked = [...aug.dtValue];
    picked[1] = 8; // 9月
    const next = normalizePicker(aug, picked);
    expect(dayColumn(next)).toHaveLength(30);
    expect(next.dtValue[2]).toBe(29);
    expect(next.dtText).toBe("2026-09-30 09:05");
  });

  it("31 日选中项切到 2 月后夹紧到 28 日", () => {
    const picked = [1, 1, 30, 9, 5]; // 2月 + 原选中「31日」下标
    const next = normalizePicker(base, picked);
    expect(dayColumn(next)).toHaveLength(28);
    expect(next.dtValue[2]).toBe(27);
    expect(next.dtText).toBe("2026-02-28 09:05");
  });

  it("年列切换同样重建日列", () => {
    const next = normalizePicker(base, [0, 1, 0, 0, 0]); // 2025-02
    expect(next.dtText).toBe("2025-02-01 00:00");
    expect(dayColumn(next)).toHaveLength(28);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/wither-e/Desktop/profo/miniapp && pnpm test test-utils/utils/datetime-picker.test.ts
```

Expected: FAIL — 报错形如 `Failed to resolve import "../../pages/viewing/utils/datetime-picker"`（模块尚不存在）。

- [ ] **Step 3: 实现 `datetime-picker.ts`**

新建 `miniapp/pages/viewing/utils/datetime-picker.ts`：

```ts
/**
 * 带看记录新增表单 · 日期时间选择器（年/月/日/时/分五列）.
 *
 * 微信 multiSelector 的列联动必须写在 bindcolumnchange 中：日列随年/月重建，
 * 否则日列会一直沿用「打开选择器时所在月份」的天数，切到更长月份时选不到月末
 * （例：9 月打开选择器，切到 8 月后日列仍只有 1~30 日，选不到 31 日）.
 */
import { pad2 } from "../../../utils/format";

/** 年列跨度（上一年～下一年，共 3 项）. */
const DT_YEAR_SPAN = 1;

/** 月/时/分列文案（固定列）. */
const DT_MONTHS = Array.from({ length: 12 }, (_, i) => `${i + 1}月`);
const DT_HOURS = Array.from({ length: 24 }, (_, i) => `${pad2(i)}时`);
const DT_MINUTES = Array.from({ length: 60 }, (_, i) => `${pad2(i)}分`);

/** 选择器状态（对应页面 data 的 dtYearStart / dtRange / dtValue / dtText）. */
export interface DateTimePickerState {
  /** 年列起始年（年列 = 起始年起 3 项）. */
  dtYearStart: number;
  /** 五列选项（年/月/日/时/分）. */
  dtRange: string[][];
  /** 五列选中下标. */
  dtValue: number[];
  /** 触发行展示文本（YYYY-MM-DD HH:mm）. */
  dtText: string;
}

/** 选择器状态中参与「日列联动」计算的部分. */
type PickerSlice = Pick<DateTimePickerState, "dtYearStart" | "dtRange" | "dtValue">;

/** 指定年月的天数（month 为 1-based）. */
function daysInMonth(year: number, month: number): number {
  return new Date(year, month, 0).getDate();
}

/** 日列文案（1~count 日）. */
function dayLabels(count: number): string[] {
  return Array.from({ length: count }, (_, i) => `${i + 1}日`);
}

/** 触发行文本（YYYY-MM-DD HH:mm）. */
function toText(year: number, value: number[]): string {
  return `${year}-${pad2(value[1] + 1)}-${pad2(value[2] + 1)} ${pad2(value[3])}:${pad2(value[4])}`;
}

/** 按给定时间构建五列选择器状态，并定位默认选中项. */
export function buildPickerState(d: Date): DateTimePickerState {
  const year = d.getFullYear();
  const start = year - DT_YEAR_SPAN;
  const value = [DT_YEAR_SPAN, d.getMonth(), d.getDate() - 1, d.getHours(), d.getMinutes()];
  return {
    dtYearStart: start,
    dtRange: [
      Array.from({ length: DT_YEAR_SPAN * 2 + 1 }, (_, i) => `${start + i}年`),
      DT_MONTHS,
      dayLabels(daysInMonth(year, d.getMonth() + 1)),
      DT_HOURS,
      DT_MINUTES,
    ],
    dtValue: value,
    dtText: toText(year, value),
  };
}

/**
 * 按选中下标归一化状态：重建日列、夹紧越界日、生成展示文本.
 *
 * `bindcolumnchange`（滚动中）与 `bindchange`（点确定）都调用本函数；
 * 前者需先把变化列的选中下标并入 picked 再传入.
 *
 * 返回完整状态（含 dtYearStart），使连续多次调用可直接把上一次结果作为下一次的 state.
 */
export function normalizePicker(state: PickerSlice, picked: number[]): DateTimePickerState {
  const year = state.dtYearStart + picked[0];
  const dim = daysInMonth(year, picked[1] + 1);
  const dtRange = [...state.dtRange];
  dtRange[2] = dayLabels(dim);
  // 原选中日可能超出新月天数（如 8-31 → 9 月），夹紧到月末
  const dtValue = [picked[0], picked[1], Math.min(picked[2], dim - 1), picked[3], picked[4]];
  return { dtYearStart: state.dtYearStart, dtRange, dtValue, dtText: toText(year, dtValue) };
}
```

> **实现期修正（2026-09-20，commit `fcc5a885`）**：本 Step 原稿把 `normalizePicker` 返回类型写成 `Pick<DateTimePickerState, "dtRange" | "dtValue" | "dtText">`（不含 `dtYearStart`），与 Step 1 测试中「把上一次结果 `aug` 再作为 state 传入」的用法冲突：`tsc` 报缺属性，且运行时 `state.dtYearStart` 为 `undefined` → `NaN` → 日列长度 0。已改为返回完整 `DateTimePickerState` 并回填 `dtYearStart`，语义为「归一化后的完整状态」，`index.ts` 的 `setData` 行为不变。上方代码块已是修正后的版本。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /Users/wither-e/Desktop/profo/miniapp && pnpm test test-utils/utils/datetime-picker.test.ts
```

Expected: PASS — 6 个用例全绿。

- [ ] **Step 5: 接线 `index.ts`**

5a. 在 [`index.ts`](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L13) 的 `import { parseSalesRecords } ...` 之后新增：

```ts
import { buildPickerState, normalizePicker, type DateTimePickerState } from "../../utils/datetime-picker";
```

5b. **删除** [第 42-57 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L42-L57) 整块（`DT_YEAR_SPAN` / `DT_MONTHS` / `DT_HOURS` / `DT_MINUTES` 常量与 `daysInMonth` / `dayLabels` 函数）。

5c. `interface PageData {` 改为 `interface PageData extends DateTimePickerState {`，并**删除**其中这 8 行（[第 99-106 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L99-L106)）：

```ts
  /** 日期时间选择器 · 年列起始年（年列 = 起始年起 3 项）. */
  dtYearStart: number;
  /** 日期时间选择器 · 五列选项（年/月/日/时/分）. */
  dtRange: string[][];
  /** 日期时间选择器 · 五列选中下标. */
  dtValue: number[];
  /** 日期时间选择器 · 触发行展示文本（YYYY-MM-DD HH:mm）. */
  dtText: string;
```

5d. `interface PageCustom` 中，在 [`onDtChange(...)`](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L125) 之前新增一行：

```ts
  onDtColumnChange(e: WechatMiniprogram.CustomEvent<{ column: number; value: number }>): void;
```

5e. 用以下代码**整体替换** [`initDateTimePicker` + `onDtChange`](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L327-L359)（原 33 行）：

```ts
  /** 按给定时间构建五列（年/月/日/时/分）选择器并定位默认选中项. */
  initDateTimePicker(d: Date): void {
    this.setData(buildPickerState(d));
  },

  /** 年/月列滚动 → 重建日列（multiSelector 联动必须走 bindcolumnchange，bindchange 只在确定时触发）. */
  onDtColumnChange(e: WechatMiniprogram.CustomEvent<{ column: number; value: number }>) {
    const { column, value } = e.detail;
    if (column !== 0 && column !== 1) {
      return;
    }
    const picked = [...this.data.dtValue];
    picked[column] = value;
    this.setData(normalizePicker(this.data, picked));
  },

  onDtChange(e: WechatMiniprogram.PickerChange) {
    this.setData(normalizePicker(this.data, e.detail.value as number[]));
  },
```

`formatRecordDate()`（[第 361-366 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.ts#L361-L366)）与 `pad2` import 保持不变。

- [ ] **Step 6: wxml 绑定 `bindcolumnchange`**

把 [`index.wxml` 第 140-147 行](file:///Users/wither-e/Desktop/profo/miniapp/pages/viewing/detail/index/index.wxml#L140-L147) 的 picker 改为：

```xml
        <picker
          mode="multiSelector"
          range="{{dtRange}}"
          value="{{dtValue}}"
          bindcolumnchange="onDtColumnChange"
          bindchange="onDtChange"
        >
```

- [ ] **Step 7: 类型检查与全量测试**

```bash
cd /Users/wither-e/Desktop/profo/miniapp && pnpm tsc && pnpm test
```

Expected: `tsc --noEmit` 零错；`vitest run` 全绿（含新增 6 个用例，且既有用例无回归）。

- [ ] **Step 8: 微信开发者工具手动验证**

打开 `pages/viewing/detail/index/index`（带看记录 → 我负责的项目 → 任一项 → 右下「＋」）：

1. 今天为 2026-09-20（9 月，30 天）。打开选择器，把「月」列滚到 `8月`。
2. **期望**：此刻「日」列立刻变为 31 项，可滚到 `31日`。
3. 选 `31日` + 任意时分 → 点「确定」→ 触发行显示 `2026-08-31 HH:mm`。
4. 点「确认添加」→ 记录出现在列表，日期为 `2026-08-31`。
5. 反向用例：把「月」列从 `8月` 滚回 `9月`，日列应立刻缩回 30 项，且不会出现「9 月 31 日」。
6. 边界用例：先选 `8月31日`，再把「月」滚到 `2月`，日列应变为 28 项且选中项落到 `28日`。
7. 年列同理（滚到上一年/下一年，日列天数随年月刷新）。

- [ ] **Step 9: 提交**

```bash
git add miniapp/pages/viewing/utils/datetime-picker.ts \
        miniapp/pages/viewing/detail/index/index.ts \
        miniapp/pages/viewing/detail/index/index.wxml \
        miniapp/test-utils/utils/datetime-picker.test.ts
git commit -m "fix(miniapp): 带看记录日期选择器日列随年月联动，修复选不到 31 日"
```

---

## 2. 完成标准

> 状态图例：✅ 已验证 ｜ ⚠️ 未验证 ｜ 已修 = 代码已提交

- [x] 后端：`extract_storage_key` 对 local / OSS 两种 URL 都能反解，对路径穿越与外部域名一律返回 `None`。✅（9 个 pytest 用例）
- [x] 后端：装修合同附件被「移除」或「替换」并保存后，旧物理文件被删除；未传字段 / 传 `null` / 删除失败 / 无法反解时均不误删、不影响保存。✅（6 个 pytest 用例，LocalStorage + tmp 目录）
- [x] 后端：全量 `964 passed, 0 failed`；`ruff check .` 与 `ruff format --check .` 无输出。✅（覆盖率门槛 68.97% < 82% 为既有问题，见 §0.4）
- [x] 前端：装修合同编辑态出现拖拽上传框，仅接受 `.xlsx/.xls/.pdf`，非白名单格式被前端拦下并 toast 提示、不发请求。✅（浏览器实测第 1-7 项）
- [ ] 前端：上传成功后保存，刷新后只读态可点「预览」打开文件；点「移除」再保存后，文件与字段一并清空。⚠️ **未验证**（需真实上传，会写生产 OSS，留待人工）
- [x] 前端：`cd frontend && pnpm typecheck && pnpm lint` 全绿。✅
- [x] miniapp：带看记录新增表单中，切到 31 天月份后日列含 `31日`。✅（6 个 vitest 用例；`pnpm tsc` 零错、501 用例全绿）
- [ ] miniapp：可在开发者工具中实际提交 `2026-08-31` 记录。⚠️ **未验证**（需真人操作开发者工具，见 Task 3 Step 8）
- [x] 数据库**零改动**（无迁移、无 `gen-api`、无新增/修改列）。✅

**三个 commit**：`379f544a`（后端）、`6548d7e3`（前端）、`fcc5a885`（miniapp）。

---

## 3. 自检（Self-Review）

**Spec 覆盖：**
- 需求 1「软装清单从保存链接改为上传，支持 Excel 和 PDF，最大 100MB」→ Task 2 Step 1（常量与校验）、Step 5（上传组件）、Step 7（端到端手动验证）。字段身份已确认（§0.1）。
- 需求 1 追加「移除时同时删除附件」→ Task 1 全流程（`extract_storage_key` + Service 保存时清理），判定规则见 §0.3，6 类分支有 pytest 覆盖。
- 需求 2「带看记录新增时选不到 31 号」→ Task 3 全流程，根因（缺 `bindcolumnchange`）与修复（Step 3/5/6）一一对应，并有 6 个回归用例（已完成）。
- 需求 2 隐含的「后端是否允许 31 号」→ 已核实允许（Task 3 根因段），无需任务。

**Placeholder 扫描：** 无 TBD/TODO；所有改动步骤均给出可直接粘贴的完整代码；所有命令均给出期望输出与预期失败/通过信息。

**类型一致性：**
- Task 1：`extract_storage_key(url: str | None) -> str | None` 在实现、`test_storage.py`、`_delete_storage_file` 三处签名一致；`_delete_storage_file(url: str) -> None` 在定义与调用处一致。
- Task 2：`SOFT_LIST_MIME_TYPES` / `SOFT_LIST_EXTENSIONS` / `SOFT_LIST_MAX_SIZE` / `validateSoftListFile` 在 Step 1 定义、Step 5 使用，命名一致；`setValue("soft_detail_attachment", ...)` 的字段名与 `contract-form/schema.ts` 的 Zod 字段一致。
- Task 3：`buildPickerState` / `normalizePicker` / `DateTimePickerState` 三处命名在测试、实现、`index.ts` 接线中完全一致；`PickerSlice` 与 `PageData extends DateTimePickerState` 结构兼容，`this.data` 可直接作为 `PickerSlice` 传入。

**跨任务依赖：** Task 2（前端「移除」按钮）与 Task 1（后端保存时删除）是同一需求的两半，**必须都完成才生效**；只做 Task 2 会出现「能清空字段但文件残留」。两者代码无耦合（前端不发删除请求），可并行开发，但验收时需一起验。

---

## 4. 风险与未覆盖（Fail Loud）

- **R1 · Nginx 100M 边界（生产）**：`client_max_body_size 100M` 与前端 100MB 限额完全相等，multipart 封装开销会让「恰好 100MB」的文件被 nginx 以 413 拒绝。**本计划不改部署配置**（既有「项目附件」功能同样存在此问题，属既有状态）。若要彻底解决，需把 nginx `client_max_body_size` 提到 `120M`，请单独确认后再改。
- **R2 · 后端无 100MB 硬限制**：绕过前端直接调 `POST /api/v1/files/upload` 仍可上传至 500MB。见 §0.2。
- **R3 · 原始文件名未持久化**：`soft_detail_attachment` 只存 URL，上传后的原始文件名（如 `8月软装清单.xlsx`）丢失，只读态只能显示「预览」链接。若要显示文件名，需加列 + 迁移 + `gen-api`，超出本次范围。
- **R4 · 反解失败时静默跳过删除**：`extract_storage_key` 是白名单式反解——DB 里若存有非本存储形态的 URL（如 OSS 模式下遗留的 `/static/uploads/...`、或历史手填的外链），删除会被跳过并打 warning，**结果是留孤儿文件，而不是误删**（刻意选择的方向）。若后续发现孤儿文件堆积，需按 URL 形态加监控。
- **R5 · 未保存的中间态会留孤儿**：见 §0.3 末尾。需要临时文件 GC 机制，不在本次范围。
- **⚠️未覆盖 · 微信开发者工具未实测**：Task 3 的根因结论基于微信官方 `picker` multiSelector 属性表、`bindchange` 仅确认时触发的事实，以及仓库内 [`pages/analysis`](file:///Users/wither-e/Desktop/profo/miniapp/pages/analysis/index/index.wxml#L29) 已有的正确写法（该页联动功能可用）。逻辑层已由单测锁定（commit `fcc5a885`，501 用例全绿），但 **wxml 事件绑定仍须在开发者工具中实测确认**（Task 3 Step 8，7 条用例）。
- **⚠️未覆盖 · 其它页面的同类缺陷**：全仓 `bindcolumnchange` 仅出现在 `pages/analysis`。本次只修 `pages/viewing/detail`；未排查其它页面是否存在自建日期/多列 picker 的同类问题（已知 Web 后台的 [`mobile-record-form.tsx`](file:///Users/wither-e/Desktop/profo/frontend/src/app/(main)/admin/projects/[projectId]/_components/mobile-record-form.tsx#L147-L161) 用 shadcn `Calendar`，无此问题）。
- **⚠️不确定 · `FileUploader` 多传一条的视觉冗余**：见 Task 2 背景说明。功能正确，仅列表可能显示两条成功记录。
- **🟡 顺带发现（既有问题，未修，超出本次范围）· NEXT_REDIRECT 被吞**：浏览器验证时 Console 稳定出现 `[ERROR] 登录页鉴权检查失败: {name: Error, message: NEXT_REDIRECT}`。根因是 [`frontend/src/app/admin/login/layout.tsx:16-21`](file:///Users/wither-e/Desktop/profo/frontend/src/app/admin/login/layout.tsx#L16-L21) 的 `redirect("/admin")` 抛出的 `NEXT_REDIRECT` 被同一函数的 `try/catch` 捕获并当作错误记日志，**未放行**——正是 AGENTS.md「NEXT_REDIRECT 守卫」条目警告的写法。后果：已登录用户访问 `/admin/login` 不会被重定向到 `/admin`（重定向被静默吞掉），只会留下一条 error 日志。与本次改动无关，建议单独修复（catch 里判断 `isRedirectError(e)` 后 `throw e`）。
