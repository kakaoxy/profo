name: Lint Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json # 缓存依赖路径

      - name: Install dependencies in frontend
        run: npm ci
        working-directory: ./frontend # 👈 关键！进入 frontend 目录

      - name: Run ESLint
        run: npm run lint
        working-directory: ./frontend # 👈 在 frontend 下运行 lint
